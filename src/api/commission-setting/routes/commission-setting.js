'use strict';

/**
 * commission-setting router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::commission-setting.commission-setting');
