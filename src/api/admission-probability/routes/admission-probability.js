'use strict';

/**
 * admission-probability router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::admission-probability.admission-probability');
